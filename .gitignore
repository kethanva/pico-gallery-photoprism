node_modules/
dist/
# The built PhotoPrism UI IS committed: the Pi appliance (512 MB) cannot run the
# webpack build, so a fresh clone must already contain the servable bundle.
!frontend/dist/
!frontend/dist/**
.cache/
*.tsbuildinfo
.env
.env.local
*.jpg
*.jpeg
*.png
*.webp
*.avif
/server/cache/
/docker/.data/
deploy-server/

# Local secrets — PhotoPrism credentials live here; never commit.
config.local.toml
# Skill outputs
graphify-out/
.cursor/*
!.cursor/rules/
!.cursor/rules/**
scripts/build-graphify.py
*.eslintcache
.eslintcache

