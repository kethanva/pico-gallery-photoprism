node_modules/
dist/
# The built PhotoPrism UI IS committed: the Pi appliance (512 MB) cannot run the
# webpack build, so a fresh clone must already contain the servable bundle.
!frontend/dist/
!frontend/dist/**
.cache/
*.tsbuildinfo
*.jpg
*.jpeg
*.png
*.webp
*.avif
/server/cache/
/docker/.data/
deploy-server/

# ── Local secrets / machine-specific config (never commit) ───────────────────
.env
.env.*
!.env.example
config.local.toml
*.local.toml
frontend/config.json
**/credentials.json
**/secrets/
*.pem
*.key
!*.example.key
id_rsa
id_rsa.pub
*.p12
*.pfx

# Skill outputs / local tooling
graphify-out/
.cursor/*
!.cursor/rules/
!.cursor/rules/**
# claude-mem auto-injects session context here; local only
.cursor/rules/claude-mem-context.mdc
scripts/build-graphify.py
*.eslintcache
.eslintcache
